name: greg_van_berkel 
description: Greg van Berkel's Professional Journey.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_flicker : ^0.52.0
  google_fonts: ^2.1.0
  flutter_markdown: ^0.6.2
  flutter_riverpod: ^0.14.0
  intl:
  url_launcher: 

dev_dependencies: 
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true 
  assets:
    - assets/cv_content/
    - assets/images/
    - google_fonts/